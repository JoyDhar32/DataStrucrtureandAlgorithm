let decimal=22;
let binary=decimal.toString(2);
let octal=decimal.toString(8);
let hexadecimal=decimal.toString(16);

console.log(` Decimal: ${decimal} \n Binary: ${binary} \n Octal: ${octal} \n Hexadecimal: ${hexadecimal}`);

// ⭐ Converting decimal to all types ⭐ 